apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: arrs-pg-superuser-creds
  namespace: media
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: arrs-pg-superuser
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      data:
        username: "{{ `{{ .username }}` }}"
        password: "{{ `{{ .password }}` }}"
  data:
    - secretKey: username
      remoteRef:
        key: postgres/superuser
        property: username
    - secretKey: password
      remoteRef:
        key: postgres/superuser
        property: password
