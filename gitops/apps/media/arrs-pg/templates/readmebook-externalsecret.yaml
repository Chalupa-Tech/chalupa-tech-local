apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: readmebook-pg-creds
  namespace: media
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: readmebook-pg
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      data:
        username: "{{ `{{ .username }}` }}"
        password: "{{ `{{ .password }}` }}"
        main_db: "{{ `{{ .main_db }}` }}"
        log_db: "{{ `{{ .log_db }}` }}"
  data:
    - secretKey: username
      remoteRef:
        key: postgres/readmebook
        property: username
    - secretKey: password
      remoteRef:
        key: postgres/readmebook
        property: password
    - secretKey: main_db
      remoteRef:
        key: postgres/readmebook
        property: main_db
    - secretKey: log_db
      remoteRef:
        key: postgres/readmebook
        property: log_db
